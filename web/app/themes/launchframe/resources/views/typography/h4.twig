<{{ as ? as : "h4" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-current" }}
        font-sans leading-none font-bold
        font-sans text-2xl md:text-3xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h4" }}>
