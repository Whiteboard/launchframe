<{{ as ? as : "h4" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-inherit" }}
        font-sans text-2xl md:text-3xl lg:text-4xl leading-tight
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h4" }}>
