<{{ as ? as : "h4" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-current" }}
        font-sans leading-tight font-bold text-2xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h4" }}>
