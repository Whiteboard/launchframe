<{{ as ? as : "h5" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-inherit" }}
        font-sans text-lg md:text-xl xl:text-2xl leading-tight
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h5" }}>
