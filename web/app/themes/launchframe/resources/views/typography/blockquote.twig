<{{ as ? as : "blockquote" }}
    class="
        {{ color ? color : "text-neutral-400" }}
        font-headline text-2xl leading-9
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "blockquote" }}>
