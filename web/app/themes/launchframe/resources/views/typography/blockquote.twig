{#
    @name Blockquote
    @desc The typography blockquote partial
    @param* content - The meta content.
    @param as - The wrapping element.
    @param color - Set a custom text color utility.
    @param class - Add custom CSS classes.
    @param attribute - Add custom JS.
    @type typography
#}

<{{ as ?: "blockquote" }}
    {{ attribute }}
    class="
        {{ color ?: 'text-neutral-400' }}
        {{ class }}
        font-headline text-2xl leading-normal italic
    "
>
    {{ content }}
</{{ as ? as : "blockquote" }}>
