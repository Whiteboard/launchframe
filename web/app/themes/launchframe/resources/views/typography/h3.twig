<{{ as ? as : "h3" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-current" }}
        font-sans leading-none font-bold
        font-sans text-3xl md:text-4xl lg:text-5xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h3" }}>
