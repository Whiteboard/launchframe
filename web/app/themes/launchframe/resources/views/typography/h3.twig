<{{ as ? as : "h3" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-current" }}
        font-sans leading-none font-bold
        text-3xl md:text-4xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h3" }}>
