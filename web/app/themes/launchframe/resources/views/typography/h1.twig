<{{ as ? as : "h1" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-inherit" }}
        font-sans leading-none font-bold
        text-4xl sm:text-5xl md:text-6xl lg:text-7xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h1" }}>
