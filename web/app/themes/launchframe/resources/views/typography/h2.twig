<{{ as ? as : "h2" }}
    {{ attribute }}
    class="
        {{ color ? color : "text-current" }}
        font-sans leading-none font-bold
        text-4xl md:text-5xl lg:text-6xl
        {{ class }}
    "
>
    {{ content }}
</{{ as ? as : "h2" }}>
