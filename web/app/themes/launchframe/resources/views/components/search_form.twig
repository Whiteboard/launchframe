{#
    @name Search Form
    @desc Primary search form for the site
    @type component
#}

<form
    role="search"
    method="get"
    action="{{ site.url }}"
    class="flex p-1 justify-start w-full"
>
    <label for="search" class="sr-only">Search</label>
    <input
        name="s"
        required
        value="{{ query }}"
        placeholder="Enter search term"
        class="
            {{ background ?: 'bg-primary' }}
            {{ color ?: ' text-white' }}
            {{ focus ?: 'focus:bg-white focus:text-primary-600' }}
            {{ placeholder ?: 'placeholder:text-white/50' }}
            flex-grow p-2 rounded-l
            border-none outline-0 focus:outline-none
            transition duration-150
        "
    />

    <button
        type="submit"
        class="
            p-4 outline-0 rounded-r
            bg-primary-400 lg:hover:bg-secondary-400 focus-visible:bg-secondary-400
            transition group
        "
    >
        <div class="w-4 lg:group-hover:scale-125 transition duration-200">
            {{ source("resources/images/search.svg") }}
        </div>
    </button>
</form>
