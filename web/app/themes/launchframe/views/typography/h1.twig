<{{ tag ? tag : "h1" }}
    class="{{ color ? color : 'text-inherit' }}
    text-6xl md:text-7xl lg:text-8xl xl:text-9xl leading-none font-bold
    {{ class }}"
>
    {{ content }}
</{{ tag ? tag : "h1" }}>
