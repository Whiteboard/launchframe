<{{ tag ? tag : "h3" }}
    class="{{ color ? color : 'text-inherit' }}
    text-4xl md:text-5xl lg:text-6xl xl:text-7xl leading-none font-bold
    {{ class }}"
>
    {{ content }}
</{{ tag ? tag : "h3" }}>
