<{{ tag ? tag : "h4" }}
    class="{{ color ? color : 'text-inherit' }}
    text-3xl md:text-4xl lg:text-5xl xl:text-6xl leading-none font-bold uppercase
    {{ class }}"
>
    {{ content }}
</{{ tag ? tag : "h4" }}>
