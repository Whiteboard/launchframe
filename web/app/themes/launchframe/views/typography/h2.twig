<{{ tag ? tag : "h2" }}
    class="{{ color ? color : 'text-inherit' }}
    text-5xl md:text-6xl lg:text-7xl xl:text-8xl leading-none font-bold
    {{ class }}"
>
    {{ content }}
</{{ tag ? tag : "h2" }}>
