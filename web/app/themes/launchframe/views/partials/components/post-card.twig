<div class="flex-grow md:flex-grow-0 w-full sm:w-1/2 lg:w-1/3 md:min-h-96 p-4 md:p-8">
    <a
        href="{{ post.link }}"
        class="flex flex-col justify-between items-start w-full h-full bg-red-500 rounded text-xl p-6 md:p-10 transition-all transition-quad lg:hover:-translate-y-2 group"
    >

        {# :: Content ------------ #}
        <div class="relative w-full">
            {# :: Title
            {+} ---------------------------------- #}
            <h4 class="text-white text-lg md:text-xl lg:text-2xl font-bold leading-tight">
                {{ post.title }}
            </h4>
        </div>
    </a>
</div>
